import type { Meta, StoryObj } from '@storybook/react'

import CoreTeamPage from './CoreTeamPage'

const meta: Meta<typeof CoreTeamPage> = {
  component: CoreTeamPage,
}

export default meta

type Story = StoryObj<typeof CoreTeamPage>

export const Primary: Story = {}
